# 项目简介
## 目标
我们现在需要设计一个向量处理器的 alu 的计算逻辑，具体来说就是对于操作数 x 是一个fp32格式的浮点数，返回exp(x)，输出也是fp32格式。
## 精度要求
对于x 在[-64,64] 之间，最大相对误差 1e-5即可
## 硬件资源消耗
我们非常在意硬件资源，需要你注意开销，所以不用的追求特别高的精度
## 时序和性能要求
我希望在8拍以内完成。
不考虑反压，每一拍都会收到一个新的x；流水线填充后，每一拍都要输出一个对应的exp(x)，即启动间隔为1拍。

### 纯前向设计原则
所有计算和流水线数据通路都必须是前向传播的，不允许产生任何feedback（反馈）路径。
不得使用依赖反馈的迭代计算、累加或算子复用方案；每个输入的数据只能沿前向数据通路传递到输出。
## 实现方式
### 1.平台
    实际上你的代码会被我手动进一步翻译成 Tensilica Interface Extension 的语句，但是我无法直接提供你这套开发环境
### 2.workaround 方法
    我们可以写verilog， 但是不是verilog的所有特性你都可以使用，你需要遵循以下原则
    1. 不要使用if 语句， 只可以使用三目运算符，例如 assign a = statement == value ? a:b;
    2. 不要使用always 语句, 也不需要任何 clk信号
    3. 设计文件采用组合逻辑实现，不实例化 TIE_FLOP；在建议打拍的位置使用注释 // please add tie pipe here 标记，由用户后续转换为 TIE 流水线。
    4. 任意位宽的乘法和加法都可以直接使用 * 和 +，后续如有必要由用户批量替换。
    5. 上述语法和时钟限制仅针对设计文件，testbench 不受这些限制。
    6. TIE 不支持 signed 声明和 $signed 转换，设计文件不得使用这些语法或有符号字面量；负数处理通过显式补码、位拼接和普通位向量运算实现。
## 验收内容
    1.设计方案文档，解释内部构造，在实际开始编写代码前可以发起一次评审
    2.verilog 设计文件1
    3.testbench 设计文件1
    4.可以运行的编译运行脚本
    5.过程中你安装的工具的安装位置，工具名称和版本，以及安装的原因
